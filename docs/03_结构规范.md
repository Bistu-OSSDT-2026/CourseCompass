# CourseCompass · 数据结构与接口规范

> 版本 v0.1 · 最后更新：Day 1

---

## 一、数据结构

### 1.1 Course（课程）

```python
class Course(db.Model):
    id           = Column(Integer, primary_key=True)
    name         = Column(String(100), nullable=False)   # 课程名称
    code         = Column(String(20), unique=True)        # 课程代码，如 CS101
    credit       = Column(Float, nullable=False)          # 学分，允许 0.5
    type         = Column(String(20))                     # 必修 / 专业选修 / 公共选修
    department   = Column(String(50))                     # 开课院系
    grade        = Column(String(20))                     # 适合年级，如 "大一,大二"
    description  = Column(Text)                           # 课程简介
    holland_tags = Column(String(100))                    # Holland 类型标签，逗号分隔
    direction    = Column(String(50))                     # 推荐方向，如 "技术/管理/研究"
    teacher_id   = Column(Integer, ForeignKey('teacher.id'))
    created_at   = Column(DateTime, default=datetime.utcnow)

    teacher      = relationship('Teacher', back_populates='courses')
    comments     = relationship('Comment', back_populates='course')
```

### 1.2 Teacher（教师）

```python
class Teacher(db.Model):
    id          = Column(Integer, primary_key=True)
    name        = Column(String(50), nullable=False)
    title       = Column(String(30))       # 教授 / 副教授 / 讲师
    department  = Column(String(50))
    research    = Column(Text)             # 研究方向，可多行
    email       = Column(String(100))      # 公开邮箱，选填
    profile_url = Column(String(200))      # 官网主页链接
    created_at  = Column(DateTime, default=datetime.utcnow)

    courses     = relationship('Course', back_populates='teacher')
```

### 1.3 Comment（评论）

```python
class Comment(db.Model):
    id          = Column(Integer, primary_key=True)
    course_id   = Column(Integer, ForeignKey('course.id'), nullable=False)
    nickname    = Column(String(30), default='匿名同学')
    content     = Column(Text, nullable=False)     # 10–500 字
    rating      = Column(Integer)                  # 综合评分 1–5 星
    difficulty  = Column(Integer)                  # 难度 1–5（1=极难，5=很轻松）
    grading     = Column(String(20))               # 给分风格：宽松 / 正常 / 严格
    semester    = Column(String(20))               # 选课学期，如 "2024-2025秋"
    is_visible  = Column(Boolean, default=False)   # 审核通过后才可见
    created_at  = Column(DateTime, default=datetime.utcnow)

    course      = relationship('Course', back_populates='comments')
```

> `is_visible` 默认 `False`，提交后进入待审核队列，由管理员运行脚本批量审核，避免垃圾内容。

### 1.4 CreditRule（学分规则）

```python
class CreditRule(db.Model):
    id          = Column(Integer, primary_key=True)
    category    = Column(String(50))    # 类别：必修 / 专业选修 / 公共选修 / 实践
    min_credit  = Column(Float)         # 该类别最低学分要求
    description = Column(String(200))   # 规则说明
    apply_to    = Column(String(20))    # 适用专业或年级
```

---

## 二、前端本地状态

### 问卷状态（`useQuiz` Hook）

```js
{
  currentIndex: 0,       // 当前题目索引 0–19
  answers: [],           // 长度 20 的数组，每项为选项值或 null
  isSubmitting: false,
  result: null           // { type: 'RI', label: '实用-研究型', directions: [...] }
}
```

### 课程筛选状态（`useCourses` Hook）

```js
{
  keyword: '',
  direction: null,         // 选中的推荐方向 id
  type: null,              // 必修 / 选修
  creditRange: [0, 6],     // 学分区间
  sort: 'credit_desc',
  page: 1
}
```

### 学分规划器（本地，不持久化）

```js
{
  grade: '',
  completed: {
    required: 0,
    majorElective: 0,
    generalElective: 0,
    practice: 0
  }
  // 差额实时计算，不存储结果
}
```

---

## 三、存储策略

| 数据 | 存储位置 | 说明 |
|------|----------|------|
| 课程 / 教师数据 | 数据库 | JSON 导入，手动维护 |
| 评论 | 数据库 | 审核通过后可见 |
| 学分规则 | `credit_rules.json` + 数据库 | 配置文件优先，方便修改不用重新部署 |
| 问卷题目 | 前端 `constants/questions.js` | 静态数据，随前端打包，减少请求 |
| Holland 映射表 | 前端 `constants/holland.js` | 纯规则，前端计算，不送后端 |
| 用户会话 | 浏览器 `sessionStorage` | 临时保存昵称，关 Tab 即清，无需注册登录 |

---

## 四、接口规范

### 4.0 通用约定

- **Base URL**：`/api/v1`
- **数据格式**：全部 JSON，`Content-Type: application/json`

**成功响应**

```json
{
  "code": 200,
  "data": { },
  "message": "success"
}
```

**错误响应**

```json
{
  "code": 400,
  "data": null,
  "message": "具体错误说明"
}
```

| HTTP 状态码 | 含义 |
|-------------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 404 | 资源不存在 |
| 500 | 服务器错误 |

---

### 4.1 问卷接口

#### `POST /api/v1/quiz/submit`

**请求**

```json
{
  "answers": ["A", "B", "A", "C", "D", "..."]
}
```

> `answers` 长度必须为 20，每项为 A / B / C / D。

**响应**

```json
{
  "code": 200,
  "data": {
    "holland_type": "RI",
    "type_label": "实用-研究型",
    "type_description": "你喜欢动手解决实际问题，同时对探索新知识有浓厚兴趣...",
    "directions": [
      {
        "id": "tech",
        "label": "技术研发方向",
        "description": "适合走工程师、算法研究路线",
        "courses": [
          { "id": 1, "name": "数据结构", "credit": 3 },
          { "id": 2, "name": "操作系统", "credit": 3 }
        ]
      }
    ]
  }
}
```

**前端调用**

```js
// api/quiz.js
export const submitQuiz = (answers) =>
  client.post('/quiz/submit', { answers })
```

---

### 4.2 课程接口

#### `GET /api/v1/courses`

**Query 参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| keyword | string | 否 | 匹配课程名 / 教师名 |
| direction | string | 否 | 推荐方向 id |
| type | string | 否 | 必修 / 专业选修 / 公共选修 |
| credit_min | float | 否 | 最低学分 |
| credit_max | float | 否 | 最高学分 |
| sort | string | 否 | `credit_desc`（默认）/ `credit_asc` / `name_asc` |
| page | int | 否 | 页码，默认 1 |
| page_size | int | 否 | 每页条数，默认 20，最大 50 |

**响应**

```json
{
  "code": 200,
  "data": {
    "total": 86,
    "page": 1,
    "page_size": 20,
    "items": [
      {
        "id": 1,
        "name": "数据结构",
        "code": "CS201",
        "credit": 3.0,
        "type": "专业必修",
        "department": "计算机学院",
        "grade": "大二",
        "direction": "技术研发",
        "avg_rating": 4.2,
        "comment_count": 15,
        "teacher": {
          "id": 5,
          "name": "张明",
          "title": "副教授"
        }
      }
    ]
  }
}
```

#### `GET /api/v1/courses/:id`

**响应（在列表基础上额外返回）**

```json
{
  "description": "本课程介绍基本数据结构与算法...",
  "holland_tags": ["R", "I"],
  "difficulty_avg": 3.8
}
```

---

### 4.3 教师接口

#### `GET /api/v1/teachers`

**Query 参数**

| 参数 | 说明 |
|------|------|
| keyword | 搜索姓名 / 研究方向 |
| department | 按院系筛选 |
| page | 页码 |

**响应 items 结构**

```json
{
  "id": 5,
  "name": "张明",
  "title": "副教授",
  "department": "计算机学院",
  "research": "机器学习、自然语言处理",
  "course_count": 2
}
```

#### `GET /api/v1/teachers/:id`

在详情基础上额外返回 `courses` 数组（该教师开设的所有课程简要信息）。

---

### 4.4 评论接口

#### `GET /api/v1/courses/:id/comments`

**Query 参数**

| 参数 | 说明 |
|------|------|
| sort | `latest`（默认）/ `highest` / `lowest` |
| page | 页码，每页 10 条 |

**响应 items 结构**

```json
{
  "id": 42,
  "nickname": "匿名同学",
  "content": "老师讲课很清楚，作业适量，期末给分正常",
  "rating": 4,
  "difficulty": 3,
  "grading": "正常",
  "semester": "2024-2025秋",
  "created_at": "2025-01-15T10:23:00Z"
}
```

#### `POST /api/v1/courses/:id/comments`

**请求**

```json
{
  "nickname": "匿名同学",
  "content": "...",
  "rating": 4,
  "difficulty": 3,
  "grading": "正常",
  "semester": "2024-2025秋"
}
```

> - `content` 必填，10–500 字
> - `rating` 必填，1–5
> - 其余字段选填
> - 提交后 `is_visible = false`，审核通过后展示

---

### 4.5 学分规划接口

#### `POST /api/v1/planner/calculate`

**请求**

```json
{
  "grade": "大一",
  "completed": {
    "required": 12,
    "major_elective": 4,
    "general_elective": 2,
    "practice": 0
  }
}
```

**响应**

```json
{
  "code": 200,
  "data": {
    "summary": {
      "required":         { "completed": 12, "required": 40, "gap": 28 },
      "major_elective":   { "completed": 4,  "required": 30, "gap": 26 },
      "general_elective": { "completed": 2,  "required": 10, "gap": 8  },
      "practice":         { "completed": 0,  "required": 8,  "gap": 8  }
    },
    "total_gap": 70,
    "recommendations": [
      {
        "category": "专业必修",
        "gap": 28,
        "suggested_courses": [
          { "id": 1, "name": "数据结构", "credit": 3 },
          { "id": 3, "name": "计算机组成原理", "credit": 3 }
        ]
      }
    ]
  }
}
```

---

## 五、前端内部函数契约

以下为纯前端模块间的函数约定，不涉及网络请求。

### `calculateHollandType(answers)`

输入 20 个答案，输出得分最高的两个 Holland 维度代码。

```js
// constants/holland.js

export const SCORE_MAP = {
  0: { A: 'R', B: 'I', C: 'A', D: 'S' },
  1: { A: 'I', B: 'R', C: 'E', D: 'C' },
  // ... 共 20 题
}

export function calculateHollandType(answers) {
  const scores = { R: 0, I: 0, A: 0, S: 0, E: 0, C: 0 }
  answers.forEach((ans, i) => {
    const dim = SCORE_MAP[i]?.[ans]
    if (dim) scores[dim]++
  })
  return Object.entries(scores)
    .sort((a, b) => b[1] - a[1])
    .slice(0, 2)
    .map(([k]) => k)
    .join('')
}
```

### `calcCreditGap(completed, rules)`

纯函数，输入已修学分和规则配置，返回各类别差额。在 `Planner.jsx` 中直接调用，不走接口。

```js
export function calcCreditGap(completed, rules) {
  return Object.fromEntries(
    rules.map(({ category, min_credit }) => [
      category,
      Math.max(0, min_credit - (completed[category] ?? 0))
    ])
  )
}
```
